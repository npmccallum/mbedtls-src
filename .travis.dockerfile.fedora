FROM @IMAGE@

RUN sudo dnf install -y cargo rustfmt make python3

ENV RUST_BACKTRACE=1

VOLUME /tmp/build
WORKDIR /tmp/build
CMD cargo fmt -- --check && cd testcrate && cargo build --verbose && cargo test --verbose
